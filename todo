basmodel as parentclass for all models
write testclass to make csv files
make first output


basecontroller for routing
-> function in controller
		-> csall the view
	view would be the tables ( check if those variables are set to preven alse code)
